{% if game.users is defined %}
    Joueurs {{game.users|length}}/{{game.maxPlayers}}
    {% for player in game.users %}
        {% if app.request.session.get('userName') == player.login %}
            <br />- <strong>{{player.login }}</strong>
        {% else %}
            <br />-{{player.login }}
        {% endif %}
    {% endfor  %}
{% endif %}